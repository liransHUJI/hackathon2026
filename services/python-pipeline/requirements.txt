# ── Core dependencies ─────────────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
# Or (preferred): pip install -e ".[dev]"

# LLM — Gemini (primary, zero-cost MVP)
google-genai>=1.0.0

# LLM — Anthropic (optional; used by LLM-Judge AI-detector later)
anthropic>=0.40.0

# Environment / config
python-dotenv>=1.0.0

# Data contracts
pydantic>=2.0
pydantic-settings>=2.0

# Async HTTP + scraping
httpx>=0.27.0
beautifulsoup4>=4.12
duckduckgo-search>=6.0

# Semantic similarity
sentence-transformers>=3.0
numpy>=1.26
scipy>=1.13

# Ingestion sources
feedparser>=6.0
python-dateutil>=2.9

# Utilities
structlog>=24.0

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
ruff
mypy
